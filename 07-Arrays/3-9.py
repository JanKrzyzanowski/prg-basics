def compare(array1, array2):
    if array1 == array2:
     return True 
    else:
       return False

array1= [
   "water", "book", "sky"
]

array2=[
   "water", "book", 'sky'
]

print(array1)
print(array2)

for text in 
if len(array1) == len(array2):
   